{
  "mcpServers": {
    "_note": "Run /career:setup to configure integrations. The wizard handles all file edits automatically."
  },
  "_inactive_services": {
    "hasdata": {
      "comment": "HasData — job search across Indeed, LinkedIn, and other boards. Configured by /career:setup.",
      "type": "stdio",
      "command": "node",
      "args": ["services/connectors/hasdata/server.js"],
      "env": {
        "HASDATA_API_KEY": ""
      }
    },
    "google-drive": {
      "comment": "Google Drive storage connector. Configured by /career:setup.",
      "type": "stdio",
      "command": "node",
      "args": ["services/connectors/google-drive/server.js"],
      "env": {
        "GOOGLE_CREDENTIALS_PATH": "services/connectors/google-drive/credentials.json"
      }
    }
  }
}
