---
description: Ingest a resume or CV into the ExperienceLibrary.
---

Run the add-source workflow for Career Navigator.

Execution requirements:
- Read and follow `skills/add-source/SKILL.md` as the source of truth.
- Use `{user_dir}` as defined by the current workspace or user-provided folder.
- Persist any library updates to `CareerNavigator/ExperienceLibrary.json`.
- Be explicit about missing files/unsupported formats rather than guessing.
