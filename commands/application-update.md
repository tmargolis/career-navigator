---
description: Process a post-update application workflow refresh.
---

Run the application-update workflow for Career Navigator.

Read and follow `skills/application-update/SKILL.md` as the source of truth, using `{user_dir}` for all reads/writes.
