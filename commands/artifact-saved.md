---
description: Reconcile saved artifacts with artifacts-index.json.
---

Run the artifact-saved workflow for Career Navigator.

Read and follow `skills/artifact-saved/SKILL.md` as the source of truth, using `{user_dir}` for all reads/writes.
