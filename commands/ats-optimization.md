---
description: Optimize resume content and structure for ATS parsing.
---

Run the ats-optimization workflow for Career Navigator.

Read and follow `skills/ats-optimization/SKILL.md` as the source of truth, using `{user_dir}` for all reads/writes.
