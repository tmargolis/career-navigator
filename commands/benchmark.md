---
description: Benchmark pipeline and profile against market norms.
---

Run the benchmark workflow for Career Navigator.

Read and follow `skills/benchmark/SKILL.md` as the source of truth, using `{user_dir}` for all reads/writes.
