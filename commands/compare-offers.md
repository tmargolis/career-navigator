---
description: Compare multiple offers with clear recommendation logic.
---

Run the compare-offers workflow for Career Navigator.

Read and follow `skills/compare-offers/SKILL.md` as the source of truth, using `{user_dir}` for all reads/writes.
