---
description: Generate a targeted cover letter for a selected role.
---

Run the cover-letter workflow for Career Navigator.

Execution requirements:
- Read and follow `skills/cover-letter/SKILL.md` as the source of truth.
- Use `{user_dir}` for all reads/writes and artifact indexing.
- Follow orchestrations/handoffs defined by the skill (including writer handoff when required).
