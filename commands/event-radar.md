---
description: Discover events across local to international scopes.
---

Run the event-radar workflow for Career Navigator.

Read and follow `skills/event-radar/SKILL.md` as the source of truth, using `{user_dir}` for all reads/writes.
