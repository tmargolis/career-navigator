---
description: Map networking paths toward target roles and companies.
---

Run the network-map workflow for Career Navigator.

Read and follow `skills/network-map/SKILL.md` as the source of truth, using `{user_dir}` for all reads/writes.
