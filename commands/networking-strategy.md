---
description: Create a networking plan with prioritized actions.
---

Run the networking-strategy workflow for Career Navigator.

Read and follow `skills/networking-strategy/SKILL.md` as the source of truth, using `{user_dir}` for all reads/writes.
