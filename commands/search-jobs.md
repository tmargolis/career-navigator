---
description: Search and rank job listings for the user profile.
---

Run the search-jobs workflow for Career Navigator.

Execution requirements:
- Read and follow `skills/search-jobs/SKILL.md` as the source of truth.
- Use `{user_dir}` profile/tracker/library context before ranking.
- If Indeed tools are unavailable, follow the skill's fallback behavior.
