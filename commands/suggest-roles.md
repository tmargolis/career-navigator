---
description: Suggest adjacent and high-probability role targets.
---

Run the suggest-roles workflow for Career Navigator.

Read and follow `skills/suggest-roles/SKILL.md` as the source of truth, using `{user_dir}` for all reads/writes.
