# career-voice local MCP server dependencies
#
# Preferred: uv handles this automatically via inline script metadata in
# scripts/voice_server.py — no manual install needed if you use uv.
#   brew install uv        (one-time, if you don't have it)
#
# If you prefer pip:
#   pip install -r requirements.txt

# MCP framework
mcp[cli]

# TTS: Kokoro with American/British English phonemizer (no espeak-ng needed)
kokoro[en]

# STT: faster-whisper (CTranslate2-based Whisper, faster + less RAM than openai-whisper)
faster-whisper

# Audio I/O
sounddevice
numpy
